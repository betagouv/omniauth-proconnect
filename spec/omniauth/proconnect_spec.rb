# frozen_string_literal: true

RSpec.describe Omniauth::Proconnect do
  it "has a version number" do
    expect(Omniauth::Proconnect::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
